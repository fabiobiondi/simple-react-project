/**
 * The board's own white.
 *
 * It lives alone because two unrelated things need it and neither should have
 * to import the other: the painter, which paints it, and the tools, where the
 * eraser is a stroke of exactly this colour.
 */
export const BOARD = '#ffffff'
